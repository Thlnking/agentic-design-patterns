{
  "name": "agentic-design-patterns-site",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:site": "node scripts/prepare-vitepress.mjs",
    "docs:dev": "npm run prepare:site && vitepress dev site",
    "docs:build:site": "npm run prepare:site && vitepress build site",
    "docs:build": "npm run docs:build:site && pagefind --site site/.vitepress/dist",
    "docs:preview": "npm run docs:build && vitepress preview site"
  },
  "devDependencies": {
    "pagefind": "^1.4.0",
    "vitepress": "^1.6.4",
    "vue": "^3.5.13"
  }
}
